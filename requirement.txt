streamlit==1.45.1
gensim==4.3.2
nltk==3.8.1
pandas==2.1.4
numpy==1.24.4  # Must stay at this version
joblib==1.3.2
scipy==1.10.1  # Critical version
--only-binary :all:  # Prevent source builds
